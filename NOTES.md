# NOTES — evidence and the owner's calls

## Evidence filed to the loom (`spools`)

Friction against the SDK, with a reproduction and our own verdict — including "not a bug, no
ask", the most common honest answer. Filed at a stranger's rank; never a feature request.

| # | friction | repro | verdict |
|---|---|---|---|
| — | nothing yet (S0 winds no spool) | | |

## Asks filed to the platform (`sofakit`)

| ticket | what | state |
|---|---|---|
| T-043 | a tape as long as the record: `DbProfile.tapeSecs`, `newTape({secs})`, the map to 93 min | PR [#96](https://github.com/osfasofa/sofakit/pull/96), on this repo's pin |

## The owner's open calls

1. **The shipped name.** `sofa` is the codename; the page says `SOFA` (`src/brand.ts`). syrup
   ships the word "sofa" for its seats feature (`docs/SOFA.md` D4, "a sofa-mate"), so if this
   product ships as SOFA, syrup's "no sibling-product reveal" scan cannot forbid the word
   without forbidding its own. Either the word is shared house vocabulary (SOFA.md §0 calls it
   the brand's root word) and both charters say so, or the product takes another name before
   anything public. spec/01 §0's collision search and trademark pass apply either way.
2. **The graph pillar**, ever (CHARTER §7).
3. **Clock-skew correction** (CHARTER §7): v0 trusts wall clocks and says when they disagree.

## Sessions

- **2026-09-07 — planning and S0.** The plan (`docs/PLAN.md`) written against syrup, sofakit
  and familiar; the owner chose the sibling repo, a Node needle, and recipes over bytes. T-043
  filed and built upstream. S0 scaffolded: the turntable boots on a 25-minute tape, lands a
  dropped file in milliseconds, scratches, refuses a longer record in words.
