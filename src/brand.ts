// The one place the shipped name lives. `sofa` is the codename — the repo,
// the databases, the storage keys, the awareness field — and the name on the
// page is this constant, so that when the owner names the product (spec/01
// §0 upstream: a collision search and a trademark pass before anything
// public) exactly one line changes. Machine talks in caps.
export const BRAND = 'SOFA';
